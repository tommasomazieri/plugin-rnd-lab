{
  "description": "dod-lite (ab-bench arm engine): hooks-only tiered Definition-of-Done checker, enforced at Stop. No skills/commands — checks are authored externally by ab-bench, never designed in-session.",
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": "node", "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/dod-check.mjs"], "timeout": 300 }
        ]
      }
    ]
  }
}
