{
  "description": "dod-lite (optimizer arm instrument): hooks-only Definition-of-Done auditor. Evaluates script and prompt checks at every Stop, records a per-turn report, and says NOTHING to the session it observes — no decision, no reason, no stdout. Checks are authored externally by optimizer, never designed in-session.",
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": "node", "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/dod-check.mjs"], "timeout": 600 }
        ]
      }
    ]
  }
}
